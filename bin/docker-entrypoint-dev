#!/usr/bin/env sh

set -e

if [ -f /data_processing_app/tmp/pids/server.pid ]; then
  rm /data_processing_app/tmp/pids/server.pid
fi

bundle install &
bundle exec rails db:create &
bundle exec rails db:migrate

exec "$@"